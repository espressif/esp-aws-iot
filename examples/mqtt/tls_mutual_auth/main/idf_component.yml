## IDF Component Manager Manifest File
dependencies:
  espressif/esp_secure_cert_mgr: "^2.8.0"
  # 3.7.9~2 is the first release with the PSA Crypto secure element
  # integration (esp_atca_psa.h), needed for ESP-IDF 6.x.
  # For local development against an unreleased esp-cryptoauthlib checkout,
  # add: override_path: "../../../../../esp-cryptoauthlib"
  espressif/esp-cryptoauthlib: "^3.7.9~2"
